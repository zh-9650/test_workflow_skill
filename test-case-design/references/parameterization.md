# Parameterization

参数化的目标是复用执行骨架，不是合并结果。

一个参数化 Template 下，每组数据都必须有独立 `instance_id` 和独立结果槽。

允许参数化的必要条件：
- 相同主动作；
- 相同前置；
- 相同断言结构；
- 只差数据或条件组合。

不适合参数化：
- 不同业务流程；
- 不同恢复机制；
- 不同并发时序；
- 一个实例失败会改变后续实例环境且无法隔离。
