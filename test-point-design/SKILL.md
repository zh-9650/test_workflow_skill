---
name: test-point-design
description: Generate and review mechanism-level test points from a confirmed structured business understanding. 仅在业务理解已完成自审、影响业务结果的问题已处理且人工确认后使用。把对象、状态、关系生命周期、业务事件、不变量、失败事实和跨模块流程转换为“可以独立失败的业务机制”级测试点；具体测试数据和执行步骤留到测试用例阶段。
---

# Test Point Design｜测试点设计

## 1. 本阶段只解决什么

本阶段只回答：

> **哪些业务机制可能独立出错，因此必须单独验证？**

输入：

```text
已人工确认的 01-business-understanding.md
+
同版本 internal/business/business-model.json
+
当前范围原始证据（只用于核对字段、来源和交互，不重新解释已经确认的业务预期）
```

输出：

```text
deliverables/02-test-points.md
deliverables/02-test-point-review.md
internal/design/test-points.json
```

测试人员主要阅读两个 Markdown；JSON 供后续结构校验和关联追踪使用。

---

## 2. 测试点的正确粒度

测试点定义：

> **一个可以独立发生正确或错误结果的业务机制。**

判断是否拆分时问：

> 这些预期是否可能彼此独立实现错？

如果一个操作同时涉及：

- 状态变化；
- 关系保留/失效；
- 下游候选变化；
- 历史事实保持不变；

且这些结果可以分别出错，就应拆成多个测试点。

反过来，下面这些不应拆成多个测试点：

- 同一长度规则的 1/2/30/31 等具体值；
- 同一数值规则的多个边界值；
- 同一等价类的多个样本；
- 只是具体用户、对象、参数取值不同，但业务机制和预期完全相同。

这些留到测试用例和执行数据组阶段。

---

## 3. 从哪里提取测试点

不要从 Markdown 标题机械生成。

必须逐项消费业务模型中的：

- 业务事件；
- 状态迁移；
- 关系生命周期；
- 业务不变量；
- 失败后最终事实；
- 下游影响；
- 模块内和跨模块流程；
- 已确认规则。

每个重要业务事实都必须有明确去向：

```text
已覆盖
不适用
明确不在本期范围
等待确认
```

不能无理由消失。

---

## 4. 提取时必须检查的机制

对每个核心业务事件按适用性检查：

### 4.1 当前动作
- 前置满足时是否允许执行；
- 成功后直接业务结果；
- 输出是否可观察。

### 4.2 输入规则
只提取规则本身，例如：
- 必填；
- 唯一；
- 格式；
- 范围；
- 当前状态是否合法；
- 引用对象是否有效。

不在这里展开具体边界数据。

### 4.3 状态
同一操作在不同状态下，如果结果不同，应分别形成测试机制。

### 4.4 关系
检查关系的：
- 建立；
- 修改；
- 失效；
- 恢复；
- 解绑；
- 删除保护。

### 4.5 下游影响与“不应变化”
上游动作发生后，分别确认：
- 哪些下游应变化；
- 哪些只失效但关系保留；
- 哪些必须保持不变；
- 历史事实是否保持不变。

### 4.6 失败后的数据一致性
有失败条件时，不只验证“报错”。按业务理解中的失败后最终事实检查：
- 主对象；
- 状态；
- 关系；
- 下游；
- 是否存在半写入、脏关系或错误统计。

### 4.7 恢复
业务定义了可恢复异常时，检查：

```text
异常
→ 修复
→ 重新进入
→ 恢复
→ 原关系/标识/历史是否连续
```

### 4.8 并发和旧状态
只对真实高风险场景考虑：
- 唯一值竞争；
- 引用变化竞争；
- 确认期间状态变化；
- 旧页面提交；
- 重复请求；
- 多人同时编辑/审批。

### 4.9 完整业务流程
原子测试点不能被完整流程替代。

如果一条长流程有独立连续性风险，可以额外增加“完整流程测试点”，但它不能替代各中间机制测试点。

---

## 5. 设计方法怎么用

等价类、边界值、判定表、状态迁移、组合覆盖等是**设计工具**，不是测试点目录。

它们主要用来检查是否漏机制：

- 有输入范围 → 后续用例阶段需要边界/等价类；
- 多条件决定结果 → 用判定表检查规则是否漏；
- 有生命周期 → 用状态迁移检查合法/非法迁移是否漏；
- 多参数交互 → 在用例阶段决定是否做组合覆盖。

不要把“边界值测试”“判定表测试”当业务模块。

---

## 6. 测试点内部结构

内部 `test-points.json` 至少记录：

```text
tp_id
module_id
business_object_ids
business_event_ids
priority
granularity
primary_coverage_id
source_coverage_ids
preconditions
mechanism
core_expected
affected_facts
must_not_change
downstream_impacts
source_status
source_refs
```

来源状态只允许：

```text
CONFIRMED
DERIVED
TEST_BASELINE
```

`DERIVED` 必须说明推导来源。

---

## 7. 测试点自审

### 7.1 粒度过粗
重点找：
- 一条测试点里出现多个可以独立失败的预期；
- 状态、关系、下游影响、历史保持被揉在一句里；
- 一整条长流程被当成一个普通测试点。

发现后拆分。

### 7.2 粒度过细
重点找：
- 只是具体数据不同；
- 只是边界实例不同；
- 只是对象样本不同；
- 业务机制和核心预期完全相同。

发现后合并到机制级测试点。

---

## 8. 本阶段发现新问题时怎么处理

测试点阶段允许确认的问题主要是：

- 本轮覆盖范围；
- 风险优先级；
- 少量颗粒度争议；
- 是否需要额外完整流程覆盖。

如果发现的问题会改变业务预期、状态、关系或业务流程，说明业务理解仍有缺口：

```text
记录问题
→ 标记受影响测试点/覆盖项
→ 返回业务理解补充
```

不要在测试点阶段自行定义产品规则。

当前可以一起确认的问题集中放到 `02-test-points.md` 末尾。每题至少包括：

- 问题；
- 为什么要确认；
- 已核对的业务理解/原始证据；
- 可选处理方式及每种方式的影响；
- 建议方案；
- 推荐原因；
- 影响范围；
- 当前状态。

用户可一次性批量回复。收到回复后更新正式测试点、审查摘要和内部 JSON，再重新自审。

---

## 9. 给测试人员看的正式测试点

`deliverables/02-test-points.md` 按业务模块 / 业务对象 / 业务事件组织。

推荐主表：

| ID | 优先级 | 业务位置/事件 | 测试点 | 前置/关键条件 | 核心预期 | 影响/不应变化 | 来源 |
|---|---|---|---|---|---|---|---|

不要把内部覆盖编号、反向映射、机器字段直接堆到主表。

文档末尾固定增加：

```text
待确认问题与建议
```

没有待确认问题也明确写“当前无需要人工确认的测试点设计问题”。

---

## 10. 测试点审查摘要

`deliverables/02-test-point-review.md` 至少说明：

- 测试点总数、P0/P1/P2；
- 各业务模块数量；
- 字段规则、状态、关系、失败、恢复、并发、完整流程、跨模块等机制数量；
- 本轮发现并修正的粒度过粗/过细问题；
- 状态迁移、关系生命周期、失败后数据一致性、恢复、并发、跨模块是否覆盖；
- 尚待确认的覆盖项；
- 需返回业务理解补充的问题；
- 建议人工重点审查的测试点；
- 当前是否建议进入测试用例生成。

---

## 11. 人工确认与进入下一阶段条件

自审与结构校验后分两种情况：

1. **仍有待确认问题**：形成“已自审、等待问题回复”的版本，但不能最终确认；涉及业务预期的问题必须返回业务理解；
2. **问题已处理**：更新正文、审查摘要和内部数据，重新自审，再提交用户最终确认。

人工确认状态只由流程状态统一记录，不在 `test-points.json` 里再保存一份确认布尔值。

测试点一旦修改，已生成的测试用例需要重新做影响分析。

---

## 12. 人工文档与内部数据同步

`02-test-points.md`、`02-test-point-review.md` 和 `test-points.json` 必须在同一轮修改中同步更新。

内部 JSON 使用 `presentation_sync` 记录本轮同步检查。最终确认时，流程同时绑定：

- 正式测试点文档；
- 测试点审查摘要；
- 内部结构化数据。

其中任一文件后续变化，都需要重新审查。

---

## 13. 结构校验

自审阶段可运行：

```bash
python test-point-design/scripts/test_point_contract.py \
  --input internal/design/test-points.json \
  --business internal/business/business-model.json \
  --allow-waiting-confirmation
```

最终确认前运行：

```bash
python test-point-design/scripts/test_point_contract.py \
  --input internal/design/test-points.json \
  --business internal/business/business-model.json
```

最终确认前必须满足：

- 业务模型版本一致；
- 所有核心业务事实都有明确覆盖去向；
- 测试点粒度自审通过；
- 没有未处理的测试点设计问题；
- 没有尚待确认的覆盖项；
- 人工文档与内部数据同步检查通过。
